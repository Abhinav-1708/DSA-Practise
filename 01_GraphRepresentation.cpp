#include <iostream>
#include <vector>
using namespace std;

int main()
{

    int n, m; // Number of nodes, Number of edges
    cin >> n >> m;
    // // Adjacency Matrix Method
    // int adj[n + 1][n + 1];

    // for (int i = 0; i < n; i++)
    // {
    //     int u, v;
    //     cin >> u >> v;
    //     adj[u][v] = 1;
    // }

    // List Method
    // vector<int> adj[n + 1];
    // for (int i = 0; i < n; i++)
    // {
    //     int u, v;
    //     cin >> u >> v;
    //     adj[u].push_back(v);
    //     adj[v].push_back(u);
    // }
}